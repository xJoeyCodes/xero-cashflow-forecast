# Ultra-minimal requirements for Windows - Guaranteed to work
# This version uses only packages with pre-built wheels

fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.25.2

# Skip these packages that require compilation:
# pydantic==2.5.0          # Requires Rust
# python-jose[cryptography] # Requires cryptography compilation
# passlib[bcrypt]          # Requires bcrypt compilation
# xero-python              # Has complex dependencies
